<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.4</version>
        <relativePath/>
    </parent>

    <groupId>com.javeriana.eventos</groupId>
    <artifactId>gestion-eventos-javeriana</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Plataforma de Gestión de Eventos Académicos - Javeriana</name>

    <modules>
        <module>shared</module>
        <module>api-gateway</module>
        <module>auth-service</module>
        <module>event-service</module>
        <module>inscription-service</module>
        <module>payment-service</module>
        <module>notification-service</module>
        <module>certificate-service</module>
    </modules>

    <properties>
        <!-- Core runtime -->
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
        <resilience4j.version>2.1.0</resilience4j.version>
        <jjwt.version>0.12.5</jjwt.version>
        <mapstruct.version>1.5.5.Final</mapstruct.version>
        <testcontainers.version>1.19.7</testcontainers.version>
        <itext.version>8.0.3</itext.version>
        <aws-sdk.version>2.25.10</aws-sdk.version>

        <!-- Quality & Documentation — pinned versions, not ranges -->
        <!-- ShedLock 5.13.0: compatible con Spring Boot 3.x + JdbcTemplate provider -->
        <!-- Justificación: ADR-018 (escalabilidad horizontal del OutboxRelayService) -->
        <shedlock.version>5.13.0</shedlock.version>
        <!-- SpringDoc OpenAPI 2.3.0: compatible con Spring Boot 3.x + OpenAPI 3.1 -->
        <!-- Justificación: SRS §11.3 (documentación de APIs entre equipos) -->
        <springdoc.version>2.3.0</springdoc.version>
        <!-- JaCoCo 0.8.11: última versión estable con soporte para Java 17/21/25 -->
        <jacoco.version>0.8.11</jacoco.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Resilience4j -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-spring-boot3</artifactId>
                <version>${resilience4j.version}</version>
            </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- MapStruct -->
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct-processor</artifactId>
                <version>${mapstruct.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- Testcontainers -->
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- iText PDF -->
            <dependency>
                <groupId>com.itextpdf</groupId>
                <artifactId>itext7-core</artifactId>
                <version>${itext.version}</version>
                <type>pom</type>
            </dependency>

            <!-- AWS SDK v2 S3 -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${aws-sdk.version}</version>
            </dependency>

            <!-- Shared module -->
            <dependency>
                <groupId>com.javeriana.eventos</groupId>
                <artifactId>shared</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- ShedLock — distributed lock para @Scheduled en multi-instancia -->
            <!-- ADR-018: previene ejecución concurrente de OutboxRelayService -->
            <dependency>
                <groupId>net.javacrumbs.shedlock</groupId>
                <artifactId>shedlock-spring</artifactId>
                <version>${shedlock.version}</version>
            </dependency>
            <!-- Provider JDBC: usa la misma BD del servicio, sin infra adicional -->
            <dependency>
                <groupId>net.javacrumbs.shedlock</groupId>
                <artifactId>shedlock-provider-jdbc-template</artifactId>
                <version>${shedlock.version}</version>
            </dependency>

            <!-- SpringDoc OpenAPI — generación automática de spec OpenAPI 3.1 -->
            <!-- SRS §11.3: documentación de APIs pública y entre microservicios -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.mapstruct</groupId>
                                <artifactId>mapstruct-processor</artifactId>
                                <version>${mapstruct.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>

                <!--
                  JaCoCo — cobertura de código por capa de arquitectura hexagonal.

                  Uso: mvn clean verify
                  Reporte: target/site/jacoco/index.html

                  Thresholds aspiracionales por capa:
                    domain         : 90% líneas, 85% ramas  (lógica de negocio pura)
                    application    : 85% líneas, 75% ramas  (casos de uso y orquestación)
                    infrastructure : 60% líneas             (adaptadores, controllers, JPA)

                  TODO[JACOCO-SOFT-MODE]: haltOnFailure=false hasta que los tests de
                  Commit 5 (OutboxRelay) y Commit 6 (E2E) estén en su lugar.
                  Cambiar a true en el commit de activación de thresholds estrictos.
                -->
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco.version}</version>
                    <configuration>
                        <!-- Clases excluidas del análisis en todos los módulos -->
                        <excludes>
                            <!-- Clase main de Spring Boot: sin lógica de negocio -->
                            <exclude>**/*Application.class</exclude>
                            <!-- Configuraciones Spring: no testables sin contexto completo -->
                            <exclude>**/config/**/*.class</exclude>
                            <!-- DTOs: clases de transferencia sin lógica -->
                            <exclude>**/dto/**/*.class</exclude>
                            <!-- Entidades JPA: mapeos sin lógica de negocio -->
                            <exclude>**/entity/**/*.class</exclude>
                        </excludes>
                    </configuration>
                    <executions>
                        <!-- Fase initialize: instrumenta bytecode para cobertura -->
                        <execution>
                            <id>prepare-agent</id>
                            <phase>initialize</phase>
                            <goals><goal>prepare-agent</goal></goals>
                        </execution>
                        <!-- Fase test: genera reporte HTML + XML en target/site/jacoco/ -->
                        <execution>
                            <id>report</id>
                            <phase>test</phase>
                            <goals><goal>report</goal></goals>
                        </execution>
                        <!-- Umbral capa domain (90% líneas, 85% ramas) -->
                        <execution>
                            <id>check-domain</id>
                            <phase>verify</phase>
                            <goals><goal>check</goal></goals>
                            <configuration>
                                <haltOnFailure>false</haltOnFailure>
                                <includes>
                                    <include>**/domain/**</include>
                                </includes>
                                <rules>
                                    <rule>
                                        <element>BUNDLE</element>
                                        <limits>
                                            <limit>
                                                <counter>LINE</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>0.90</minimum>
                                            </limit>
                                            <limit>
                                                <counter>BRANCH</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>0.85</minimum>
                                            </limit>
                                        </limits>
                                    </rule>
                                </rules>
                            </configuration>
                        </execution>
                        <!-- Umbral capa application (85% líneas, 75% ramas) -->
                        <execution>
                            <id>check-application</id>
                            <phase>verify</phase>
                            <goals><goal>check</goal></goals>
                            <configuration>
                                <haltOnFailure>false</haltOnFailure>
                                <includes>
                                    <include>**/application/**</include>
                                </includes>
                                <rules>
                                    <rule>
                                        <element>BUNDLE</element>
                                        <limits>
                                            <limit>
                                                <counter>LINE</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>0.85</minimum>
                                            </limit>
                                            <limit>
                                                <counter>BRANCH</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>0.75</minimum>
                                            </limit>
                                        </limits>
                                    </rule>
                                </rules>
                            </configuration>
                        </execution>
                        <!-- Umbral capa infrastructure (60% líneas) — excluido del fail -->
                        <execution>
                            <id>check-infrastructure</id>
                            <phase>verify</phase>
                            <goals><goal>check</goal></goals>
                            <configuration>
                                <haltOnFailure>false</haltOnFailure>
                                <includes>
                                    <include>**/infrastructure/**</include>
                                </includes>
                                <rules>
                                    <rule>
                                        <element>BUNDLE</element>
                                        <limits>
                                            <limit>
                                                <counter>LINE</counter>
                                                <value>COVEREDRATIO</value>
                                                <minimum>0.60</minimum>
                                            </limit>
                                        </limits>
                                    </rule>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
