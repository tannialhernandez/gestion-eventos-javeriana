# Registry local del frontend para evitar el DNS bloqueado de registry.npmjs.org.
# El registry de Yarn es un mirror publico del ecosistema npm y no cambia el stack.
registry=https://registry.yarnpkg.com/
@playwright:registry=https://registry.yarnpkg.com/
